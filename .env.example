# Public endpoint only. Never embed a privileged Postgres DATABASE_URL in the desktop bundle.
# This is the HTTPS Neon database URL used by @neondatabase/neon-js, without credentials.
VITE_NEON_DATABASE_URL=
